To make a new release:

* make sure everything is commited
* mvn3 release:prepare release:perform -Darguments=-Dsign.storepass=$smart_card_pin
* if happy with the release: git push && git push --tags
